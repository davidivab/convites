<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            CRÍTICO: force="true" es obligatorio en DB_DATABASE. docker-compose.yml
            inyecta DB_DATABASE=convites (la base real de dev) como variable de
            entorno del contenedor; sin force="true" PHPUnit NO pisa una env var
            que ya existe en el proceso, así que "docker compose exec app php
            artisan test" terminaba corriendo RefreshDatabase (migrate:fresh)
            contra la base real y la vaciaba. DB_HOST/DB_PORT/credenciales se
            heredan del entorno que lanza el proceso (host.docker.internal en
            docker, 127.0.0.1 nativo) porque el servidor MySQL es el mismo en
            ambos casos — solo el nombre de la base debe forzarse.
        -->
        <env name="DB_CONNECTION" value="mysql"/>
        <env name="DB_DATABASE" value="convites_test" force="true"/>
        <env name="DB_URL" value="" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
